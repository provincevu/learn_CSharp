/*
## **NGÀY 5: OOP nâng cao (Kế thừa, Interface, Polymorphism)**

### **Bài 1:**  
Tạo class `Animal` (name, sound), các class kế thừa (Dog, Cat), mỗi class override hàm Speak().

### **Bài 2:**  
Tạo interface `IMovable` với hàm Move(). Các class `Car`, `Bike`, `Person` implement interface này, mỗi loại move khác nhau.

### **Bài 3:**  
Tạo danh sách các đối tượng (Animal, Car, Person...), gọi hàm hành động (Speak/Move) bằng tính đa hình.

### **Bài 4:**  
Tạo class `Shape` (abstract), các class kế thừa: `Circle`, `Rectangle`, `Triangle`. Viết hàm tính diện tích, chu vi từng loại.

### **Bài 5:**  
Tạo class `Employee` (base), class `Manager` kế thừa Employee, thêm thuộc tính bonus, override hàm tính lương.
*/